### Scss `sass-lang.com` - Un préprocesseur css

- Pour l'installation
    > On peut le faire via une application
    > !! en ligne de commande en mode standolone, télécharger l'archive `dart-sass` sur `github` qui va contenir l'exe pour travailler avec sass
- npm install -D sass : Installer sass en dépendance

- **Important !**
    > Le fichier `map` permet d'indiquer au navigateur la correspondance entre une ligne css et une ligne scss
    > Pour grouper les médias query qu'on aurais mis partout dans le code on peut passer par un autre préprocesseur
    > Le commentaire avec double slash n'est pas compilé