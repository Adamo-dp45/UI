<script setup>
/* -- import emailjs from 'emailjs-com'
    emailjs.send('service_xxx', 'template_xxx', {
        from_name: form.value.name,
        from_email: form.value.email,
        message: form.value.message
    }, 'public_key_xxx')
    .then(() => {
        form.value = { name: '', email: '', message: '' } -- Success
    })
    .catch(() => {
        -- Error
    })
    .finally(() => {
        loading.value = false
    })
    -- On remplace service_xxx, template_xxx, public_key_xxx par ceux d’emailjs
*/
</script>

<template>
    <h1>Contact</h1>
</template>